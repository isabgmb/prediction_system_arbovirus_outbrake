# ── Database ──────────────────────────────────────────────────────────────────
# SQLite — local testing, no Docker needed
DATABASE_URL=sqlite+aiosqlite:///./data/sipsa_test.db

# PostgreSQL — swap this in when ready for production
# DATABASE_URL=postgresql+asyncpg://sipsa:sipsa123@localhost:5432/sipsa

# ── Auth ──────────────────────────────────────────────────────────────────────
SECRET_KEY=local-dev-secret-change-in-production

# ── Model ─────────────────────────────────────────────────────────────────────
MODEL_WEIGHTS_PATH=data/models/eval_results.npz
